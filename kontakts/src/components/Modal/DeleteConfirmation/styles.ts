import { styled } from 'styled-components';

const StyledSectionDelete = styled.section`
  padding: 16px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 48px;
  height: 16rem;
  text-align: center;

  button {
    background-color: var(--color-negative);
    color: var(--color-grey-0);
  }
`;

export default StyledSectionDelete;
